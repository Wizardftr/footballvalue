# Copy to .env and fill in. .env is gitignored and must never be committed.

# The Odds API (https://the-odds-api.com) - only needed from Phase 4 onward.
ODDS_API_KEY=

# Optional: override the SQLite location (default: data/footballvalue.db)
# FV_DB_PATH=data/footballvalue.db
